struct complex
{
	float real;
	float img;
};

int save(struct complex c) {
	// Ecrire le code de la fonction permettant de sauvegarder c dans le fichier "test_write.cpx" 
	// renvoyer -1 en cas d'erreur
	// renvoyer 1 si la structure a été écrite dans le fichier
}