#include <stdio.h>
#include <stdlib.h>

struct complex
{
	float real;
	float img;
};
int get_struct_nbr(char * nom)
{
	//Ecrire le code permettant de renvoyer le nombre d'enregistrements présents dans le fichier dont le nom est contenu dans le paramètre nom
	// Renvoyer ce nombre ou -1 en cas d'erreur
	
}